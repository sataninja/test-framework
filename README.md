```
Arguments:

Browser options available: chrome, firefox (chrome by default)
-Dbrowser=chrome

Environments available: development, staging (by default);
-Denv=staging

Retry analyzer, false by default
-Dretry=false

Admin user email and password
-Duser.email.admin=your@email.com
-Duser.password.admin=password

Simple user email and password
-Duser.email.member=your@email.com
-Duser.password.member=password

EMAIL properties used for connecting a specified user to his inbox folder
-Demail.address=your@email.com
-Demail.password=password

```

